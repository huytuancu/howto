# create <>.yaml files. See pod.yaml, deployment.yaml, service.yanl, ingress.yaml examples in links below

# create a k8s namespace

# create a volume definition for the pods

pod.yaml
deployment.yaml
# service.yanl
key point is to bind to a port

ingress.yaml 
key point is to define DNS and TLS certificates etc


# use kubectl commands to apply into the cluster
kubectl appy -f <>.yaml

# pod.yaml example
https://kubernetes.io/docs/concepts/workloads/pods/

# Deployment.yaml example
https://kubernetes.io/docs/concepts/workloads/controllers/deployment/

# service and ingress example:
https://kubernetes.io/docs/concepts/services-networking/service/
https://kubernetes.io/docs/concepts/services-networking/ingress/

# check ingress/DNS
kubectl describe ingress <ingress name>

# more?

# this is an example of Dockerfile for python
FROM

# get environment variables using ARG
ARG Product_Number
ARG Product_Version

LABEL \

ENV \

# execute command using RUN
# eg:
RUN 

# run the app.py
CMD ["python3", "app/app.py"]

# wget
wget -c 'https://github.com/kubernetes/minikube/releases/download/v1.24.0/minikube-linux-amd64'

# export new PATH to current PATH
eg:
echo PATH
export PATH=/usr/local/bin/:$PATH

# sed command - search and replace. very useful

# set +e
# set -e
set -e stops the execution of a script if a command or pipeline has an error

# ps -ef | grep <your wanted process/application>

# du -ah /home/$USER --> check current storage ìnformation

# find and print correct lines
find . -name \*.* |xargs grep -n "my needed string to search" 

# list available modules
module av list

# curl - download test.tar file
curl -O https://<>/.../test.tar
tar -xvf test.tar

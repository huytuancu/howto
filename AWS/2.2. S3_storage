# S3 is used to store files as AWS storage solution from user 's choice

key topics:

- storage time:

30 days
90 days
objects reaching lifecycle endpoint can be automatically deleted

- S3 event notifications

- Requester Pays (data transfer pay for downloader) etc

- Security

- Advanced Tools (Datasync, storate lens, batch operations)

- a storage instance (important to know)
eg:

development_instance # shall be test/staging/production
  ENDPOINT: "https://..."
  BUCKET_NAME: "your-bucket-name" # can understand as folder name
  AWS_REGION: "your_aws_region" # eg: eu-est-001 

# https://www.w3schools.com/typescript/
# typescript with nodejs
https://www.w3schools.com/typescript/typescript_nodejs.php

# typescript is maintained and updated by Microsoft
# typescript is used to write then it will convert to javascript automatically
# typescript is needed for node.js dev projects

# tutorial once you want to re-check on specific topic
https://www.w3schools.com/typescript/

# time in mili seconds
eg:
const TIME_MS = 100_000; # _ is a separate method. In this case it is 100000 milli seconds

# .ts vs .tsx
.ts --> normal typescript eg: write simple functions
.tsx --> can inject JSX aka doing React or interact as web tags to visualize GUI

# initiate a new object
eg:
const myUser = new templateUser({ # templateUser should be defined somewhere
...
});


# describe
usually used in .test.ts
collects test cases into one group

# && AND !== NOT EQUAL

# boolean variable
eg:
truaOrFalse?: boolean;

# latest typescript version
5.x

# key notes
it is not easy to remember rules in writing typescript, just understand the idea of logic then search AI for faster 

# file name
eg: typescript.ts

# import import standard , plugin , self defined libs etc
eg:

impport { randomBytes } from 'node:crypto';
import { myFunction } from './myLib'; # myLib.ts is introduced

# constant --> define variable

# function --> standard function, is used on current file

# export function --> the function can be exported to be used by other files

# let --> define local variable 
eg:
let stderr = '';

# await --> polling

# .toString('base64') --> convert the string to base64

# expect --> expect return to match
eg:
expect(logs).toContain(' ready ')

# export async function <my function> --> self defined function
# at least need to write unit test for each function/class --> check chatgpt for examples

# async function --> function to processing asynchronous aka can use await inside

# throw new
eg:
throw new InputError('user passing wrong variable');

# "export interface < > " can be used from other files

# interface --> an object in typescript
eg:
interface User {
  id: number;
  name: string;
  email: string;
}

then declare:

const user: User = {
  id: 1,
  name: "Max",
  email: "max0987654321@gmail.com"
};

# important libs from node.js
import * as os from 'node.os' # operating libs
import * as fs from 'node.fs' # file system libs
import * as path from 'node.js' # path

